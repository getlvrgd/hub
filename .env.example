# A Postgres for the Hub. Vercel's Neon integration sets both of these for you.
DATABASE_URL="postgresql://…"

# Signs the session cookie. 32+ characters. The app refuses to start without it.
# Generate one:  openssl rand -base64 32
AUTH_SECRET=""
